/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['intellectx.s3.amazonaws.com',"intellectx.s3.ap-south-1.amazonaws.com"],
      },
}

module.exports = nextConfig
